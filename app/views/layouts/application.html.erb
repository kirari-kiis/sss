<!DOCTYPE html>
<html>
<link href="https://use.fontawesome.com/releases/v5.6.1/css/all.css" rel="stylesheet">
<link href="https://fonts.googleapis.com/css?family=Comfortaa:600&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css?family=Comfortaa:600|M+PLUS+Rounded+1c&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css?family=Quicksand&display=swap" rel="stylesheet">

  <head>
    <title>ChallecaraApp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <header >
  <%=link_to("SSS","/home/top")%>

<!--  <% if current_user %> <%# 追加しました %>
    <%=link_to("マイページ", user_path(current_user))%>
  <% else %>
    <%=link_to("マイページ", "/home/top") %>
  <% end %>

  <%=link_to("教科一覧", "/subjects")%>-->

  </header>




  <div class="menu-trigger" href="">
      <span></span>
      <span></span>
      <span></span>
    </div>
    <nav>

        <%=link_to("HOME","/home/top")%><br><br>
        <% if current_user %> <%# 追加しました %>
            <%=link_to("マイページ", user_path(current_user))%>
          <% else %>
            <%=link_to("マイページ", "/home/top") %>
          <% end %><br><br>
        <%=link_to("教科一覧", "/subjects")%><br><br>

        <% if current_user %>
        <%= link_to( "ログアウト", destroy_user_session_path, method: "delete") %>
        <%else%>
        <%= link_to "Sign up", new_registration_path(resource_name) %><br/><br>
        <%= link_to "Forgot your password?", new_password_path(resource_name) %><br />
        <%end%>


    </nav>
    <div class="overlay"></div>



<script type="text/javascript">

  $('.menu-trigger').on('click',function(){
    if($(this).hasClass('active')){
      $(this).removeClass('active');
      $('nav').removeClass('open');
      $('.overlay').removeClass('open');
    } else {
      $(this).addClass('active');
      $('nav').addClass('open');
      $('.overlay').addClass('open');
    }
  });
  $('.overlay').on('click',function(){
    if($(this).hasClass('open')){
      $(this).removeClass('open');
      $('.menu-trigger').removeClass('active');
      $('nav').removeClass('open');
    }
  });
</script>






   <!-- <div class="side">
  <%=link_to("HOME","/home/top")%>
  <br>
  <% if current_user %> <%# 追加しました %>
    <%=link_to("マイページ", user_path(current_user))%>
  <% else %>
    <%=link_to("マイページ", "/home/top") %>
  <% end %>
  <br>
    <%=link_to("教科一覧", "/subjects")%>

  <br>
  <%= link_to( "ログアウト", destroy_user_session_path, method: "delete") %>
  <br>
  <% if current_user %>
  <%else%>
  <%= render "users/shared/links" %>
  <%end%>
  </div> -->


  <body>
    <%= yield %>
  </body>


</html>
